name: Deploy via ftp
on: push
jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Copy via rsync
        # You may pin to the exact commit or the version.
        uses: kroominator/rsync-deployments-simply-com@6.0.1
        with:
          switches: -avzr --delete
          path: ./
          remote_path: /public_html/labs/
          remote_host: linux347.unoeuro.com
          remote_user: ginkru.dk
          remote_password: Slaptazodis29
          remote_key: ${{ secrets.SSH_PRIVKEY }}
